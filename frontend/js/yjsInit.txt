Y({
  db: {
    name: 'memory'
  },
  connector: {
    name: 'websockets-client',
    room: 'cae-room'
  },
  share: {
    $Variable_Init$
  }
}).then(function (y) {
  window.yTextarea = y

  $Share_Element$

})

